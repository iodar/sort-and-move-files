# Group and Move Items

### Prerequisites

- pwsh >= 7.x
- tests: pester >=4.10.x

### Run Tests

Using `bash`

```bash
./test.sh [--coverage]
```

Using `pwsh`

```powershell
./test.ps1 [-Coverage]
```